# 🏗️ Arquitetura do Sistema - Books API

## Visão Geral

Este documento detalha a arquitetura do sistema Books API, incluindo componentes, fluxo de dados, decisões de design e considerações para escalabilidade futura.

## 📐 Diagrama Arquitetural

```
┌─────────────────────────────────────────────────────────────────┐
│                        CAMADA DE CLIENTE                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Cientista  │  │  Aplicação   │  │   Modelo ML  │         │
│  │   de Dados   │  │     Web      │  │              │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                  │                  │                  │
│         └──────────────────┼──────────────────┘                  │
│                            │                                     │
└────────────────────────────┼─────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      CAMADA DE API (FastAPI)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Middleware Layer                       │  │
│  │  • CORS  • Logging  • Error Handling  • Auth JWT        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─────────┬─────────┬─────────┬─────────┬─────────┬────────┐ │
│  │ Books   │Category │ Stats   │ Health  │  Auth   │   ML   │ │
│  │ Router  │ Router  │ Router  │ Router  │ Router  │ Router │ │
│  └─────────┴─────────┴─────────┴─────────┴─────────┴────────┘ │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Pydantic Models (Validation)                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     CAMADA DE DADOS                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │            BooksDatabase (Data Manager)                   │  │
│  │  • Query Engine  • Filtering  • Aggregation              │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                            │
│                     ▼                                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   CSV Storage                             │  │
│  │               (data/books.csv)                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▲
┌────────────────────────────┼────────────────────────────────────┐
│                     CAMADA DE INGESTÃO                           │
├────────────────────────────┼────────────────────────────────────┤
│                            │                                     │
│  ┌─────────────────────────┴──────────────────────────────┐    │
│  │               Web Scraper (BeautifulSoup)              │    │
│  │  • HTTP Client  • Parser  • Data Extraction            │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│                            ▼                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              books.toscrape.com                          │  │
│  │           (Fonte Externa de Dados)                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  CAMADA DE OBSERVABILIDADE                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────┐             │
│  │ Structured Logs  │         │    Metrics       │             │
│  │   (JSON Format)  │         │ (Process Time)   │             │
│  └──────────────────┘         └──────────────────┘             │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo de Dados

### 1. Pipeline de Ingestão

```
┌─────────────┐
│   Trigger   │ (Manual ou Scheduled)
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│  Web Scraper        │
│  1. HTTP Request    │
│  2. HTML Parsing    │
│  3. Data Extraction │
│  4. Transformation  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Validation &       │
│  Cleaning           │
│  • Remove nulls     │
│  • Normalize prices │
│  • Encode ratings   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  CSV Storage        │
│  • Append mode      │
│  • UTF-8 encoding   │
│  • Header included  │
└─────────────────────┘
```

### 2. Pipeline de Consulta

```
┌─────────────┐
│  HTTP GET   │
│  /api/v1/*  │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│  Middleware         │
│  • CORS             │
│  • Logging          │
│  • Auth (se req.)   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Router             │
│  • Parse params     │
│  • Validate input   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Data Manager       │
│  • Load CSV         │
│  • Apply filters    │
│  • Paginate         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Pydantic Models    │
│  • Serialize        │
│  • Validate output  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  JSON Response      │
│  • 200 OK           │
│  • Process-Time     │
└─────────────────────┘
```

### 3. Pipeline ML

```
┌─────────────┐
│  ML Client  │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│  GET /ml/features   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Feature Eng.       │
│  • Normalize        │
│  • Encode           │
│  • Transform        │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Training Data      │
│  • X (features)     │
│  • y (target)       │
│  • metadata         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  ML Model           │
│  • Train            │
│  • Validate         │
│  • Deploy           │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  POST /predictions  │
│  • Store results    │
│  • Track metrics    │
└─────────────────────┘
```

## 🧩 Componentes Principais

### 1. Web Scraper (`scripts/scraper.py`)

**Responsabilidades:**
- Extração de dados do site Books to Scrape
- Parsing de HTML com BeautifulSoup
- Transformação e limpeza de dados
- Armazenamento em CSV

**Características:**
- Rate limiting (pausa entre requisições)
- Error handling robusto
- Logging detalhado
- Suporte a múltiplas páginas e categorias

**Tecnologias:**
- `requests`: HTTP client
- `beautifulsoup4`: HTML parsing
- `pandas`: Data manipulation
- `lxml`: XML/HTML parser

### 2. API Layer (`api/`)

**FastAPI Application:**
- Framework assíncrono
- Validação automática com Pydantic
- Documentação Swagger automática
- Performance otimizada

**Routers:**
- `books.py`: CRUD e busca de livros
- `categories.py`: Gestão de categorias
- `stats.py`: Estatísticas e insights
- `health.py`: Health checks
- `auth.py`: Autenticação JWT
- `ml.py`: Endpoints ML-Ready
- `scraping.py`: Admin endpoints

### 3. Data Manager (`api/database.py`)

**Responsabilidades:**
- Carregamento e cache de dados CSV
- Query engine para filtros complexos
- Agregações e estatísticas
- Paginação eficiente

**Design Pattern:**
- Singleton pattern para instância única
- Lazy loading dos dados
- In-memory cache com Pandas DataFrame

### 4. Authentication (`api/auth.py`)

**Sistema JWT:**
- Tokens com expiração configurável
- Refresh token support
- Password hashing com bcrypt
- Role-based access (futuro)

**Segurança:**
- HTTPS recommended (produção)
- Secret key rotation
- Token blacklist (futuro)

## 🔒 Segurança

### Camadas de Segurança

```
┌─────────────────────────────────┐
│     1. Transport Layer          │
│        HTTPS / TLS              │
└────────────┬────────────────────┘
             │
┌────────────▼────────────────────┐
│     2. Authentication           │
│        JWT Tokens               │
└────────────┬────────────────────┘
             │
┌────────────▼────────────────────┐
│     3. Authorization            │
│   Route-based Access Control    │
└────────────┬────────────────────┘
             │
┌────────────▼────────────────────┐
│     4. Validation               │
│     Pydantic Models             │
└────────────┬────────────────────┘
             │
┌────────────▼────────────────────┐
│     5. Rate Limiting            │
│    (Future Implementation)      │
└─────────────────────────────────┘
```

### Boas Práticas Implementadas

- ✅ Variáveis de ambiente para secrets
- ✅ Password hashing (bcrypt)
- ✅ Token expiration
- ✅ CORS configurável
- ✅ Input validation
- ✅ Error handling sem exposição de internals
- ✅ Structured logging

## 📊 Escalabilidade

### Arquitetura Atual (MVP)

```
┌──────────────┐
│   FastAPI    │
│   (Single)   │
└──────┬───────┘
       │
┌──────▼───────┐
│  CSV File    │
│  (Local)     │
└──────────────┘
```

**Limitações:**
- Single instance
- In-memory data
- File-based storage
- No horizontal scaling

### Arquitetura Futura (Escalável)

```
┌───────────────────────────────────────┐
│         Load Balancer (Nginx)         │
└────────────┬──────────────────────────┘
             │
     ┌───────┴───────┐
     │               │
┌────▼────┐    ┌────▼────┐
│ FastAPI │    │ FastAPI │  (Multiple Instances)
│ Pod 1   │    │ Pod 2   │
└────┬────┘    └────┬────┘
     │               │
     └───────┬───────┘
             │
┌────────────▼──────────────┐
│     Redis Cache           │
│  (Shared Cache Layer)     │
└────────────┬──────────────┘
             │
┌────────────▼──────────────┐
│   PostgreSQL Database     │
│  (Persistent Storage)     │
└───────────────────────────┘

┌───────────────────────────┐
│   Background Workers      │
│  (Celery + Redis)         │
│  • Scraping tasks         │
│  • Data processing        │
└───────────────────────────┘
```

### Plano de Evolução

#### Fase 1: Otimização Atual
- ✅ Implementar cache em memória
- ✅ Otimizar queries pandas
- ✅ Adicionar índices no DataFrame

#### Fase 2: Banco de Dados
- [ ] Migrar para PostgreSQL
- [ ] Implementar SQLAlchemy ORM
- [ ] Schema versioning com Alembic
- [ ] Connection pooling

#### Fase 3: Cache Layer
- [ ] Implementar Redis
- [ ] Cache de queries frequentes
- [ ] Session storage
- [ ] Rate limiting storage

#### Fase 4: Horizontal Scaling
- [ ] Containerização (Docker)
- [ ] Orchestration (Kubernetes)
- [ ] Load balancing
- [ ] Auto-scaling policies

#### Fase 5: Processamento Assíncrono
- [ ] Message queue (RabbitMQ/Redis)
- [ ] Celery workers
- [ ] Distributed scraping
- [ ] Background data processing

#### Fase 6: Observabilidade Avançada
- [ ] Prometheus metrics
- [ ] Grafana dashboards
- [ ] ELK Stack para logs
- [ ] Distributed tracing (Jaeger)

## 🎯 Casos de Uso para ML

### 1. Sistema de Recomendação

```python
# Algoritmo baseado em conteúdo
def recommend_books(user_preferences):
    # 1. Obter features
    features = api.get('/ml/features')
    
    # 2. Calcular similaridade
    similar = calculate_similarity(
        user_preferences,
        features
    )
    
    # 3. Ranquear e retornar
    return top_k_similar(similar, k=10)
```

### 2. Predição de Preços

```python
# Modelo de regressão
def predict_price(book_features):
    # 1. Treinar modelo
    training_data = api.get('/ml/training-data')
    model = train_price_predictor(training_data)
    
    # 2. Fazer predição
    predicted_price = model.predict(book_features)
    
    # 3. Submeter resultado
    api.post('/ml/predictions', {
        'book_id': book_id,
        'prediction': predicted_price
    })
```

### 3. Análise de Sentimento (Futuro)

```python
# Análise de reviews (quando disponível)
def analyze_reviews():
    books = api.get('/books')
    for book in books:
        sentiment = sentiment_model.predict(
            book['description']
        )
        update_sentiment_score(book, sentiment)
```

## 🔄 Integração com Modelos ML

### API Contract

```python
# Input Format
{
    "features": [
        {
            "id": 1,
            "price_normalized": 0.45,
            "rating_normalized": 0.8,
            "category_encoded": 5,
            "in_stock": true
        }
    ]
}

# Output Format
{
    "predictions": [
        {
            "book_id": 1,
            "prediction": 4.2,
            "confidence": 0.85,
            "model_version": "v1.0"
        }
    ]
}
```

### Feature Engineering Pipeline

```
Raw Data → Cleaning → Normalization → Encoding → Features
    ↓
  CSV File
    ↓
API Endpoint (/ml/features)
    ↓
ML Model Training
    ↓
Model Deployment
    ↓
Predictions API
```

## 📈 Métricas e Monitoramento

### Métricas de Performance

- **Response Time**: < 100ms (p95)
- **Throughput**: 1000 req/s (target)
- **Error Rate**: < 0.1%
- **Availability**: 99.9%

### Logs Estruturados

```json
{
  "timestamp": "2025-11-02T10:30:00.123Z",
  "level": "INFO",
  "service": "books-api",
  "endpoint": "/api/v1/books",
  "method": "GET",
  "status_code": 200,
  "process_time_ms": 45,
  "user_id": "admin",
  "request_id": "abc-123-def"
}
```

## 🚀 Deployment Pipeline

```
┌─────────────┐
│  Git Push   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  CI/CD      │
│  • Tests    │
│  • Lint     │
│  • Build    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Container  │
│  Registry   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Deploy     │
│  • Staging  │
│  • Prod     │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Monitor    │
│  • Health   │
│  • Metrics  │
└─────────────┘
```

## 🎓 Decisões de Design

### Por que FastAPI?

✅ **Performance**: Assíncrono, baseado em ASGI
✅ **Developer Experience**: Type hints, auto-documentation
✅ **Validation**: Pydantic built-in
✅ **Modern**: Python 3.11+ features
✅ **Comunidade**: Grande e ativa

### Por que CSV (inicialmente)?

✅ **Simplicidade**: Fácil de implementar
✅ **Portabilidade**: Funciona em qualquer ambiente
✅ **Debug**: Fácil de inspecionar
✅ **MVP**: Rápido time-to-market

❌ **Limitação**: Não escala para produção
→ **Solução**: Migração para PostgreSQL planejada

### Por que JWT?

✅ **Stateless**: Não requer session storage
✅ **Escalável**: Funciona em múltiplos servidores
✅ **Standard**: RFC 7519
✅ **Flexível**: Claims customizáveis

## 📚 Referências

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Pydantic V2](https://docs.pydantic.dev/)
- [JWT Best Practices](https://tools.ietf.org/html/rfc8725)
- [REST API Design](https://restfulapi.net/)
- [12 Factor App](https://12factor.net/)

---

**Documento mantido por**: Tech Challenge Team
**Última atualização**: 2025-11-02
**Versão**: 1.0



